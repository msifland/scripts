#!/bin/bash
# This is a blank bash script document.
# Put your code below this line.
echo
echo
echo "${ILCOLOR2}This updates the Kate .desktop file in /usr/share/applications so Kate opens files in new window."
echo
sleep 1
# Set file variables.
FILE="/usr/share/applications/org.kde.kate.desktop"

# Look in kate file for text and change if it's  there.
PW='spike'
if [[ $(echo $PW | sudo -S grep 'Exec=kate -b %U' $FILE) ]]; then
	sudo sed -i 's:Exec=kate -b %U:Exec=kate -b -n %U:g' $FILE
	echo
	echo
	echo "Kate file was updated."
	echo
	sleep 1
else
	echo
	echo
	echo "No update needed. All is good."
	echo
	sleep 1
#fi
fi
# Write Kate file modification date to kate-date file.
echo "Done!!!${ILRESTORE}"
sleep 1
exit